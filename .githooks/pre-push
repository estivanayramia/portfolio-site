#!/bin/sh
# Pre-push hook: block pushes that fail core perf gates
set -eu

echo "🔎 Pre-push: verifying versioning..."
npm run -s verify:versioning

echo "🧪 Pre-push: running audit gate..."
npm run -s audit

echo "🎞️ Pre-push: running animation jank gate..."
npm run -s anim:jank
