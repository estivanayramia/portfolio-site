// Deprecated script.
// This repository no longer uses an /en prefix and canonical/og:url values must be extensionless.
// The old behavior would regress SEO, so this script is intentionally disabled.

console.error('scripts/fix-seo-metadata.js is deprecated and intentionally disabled.');
console.error('Canonical and OG URLs must remain extensionless and without language prefixes.');
process.exit(1);
