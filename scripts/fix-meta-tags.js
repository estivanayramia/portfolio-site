// Deprecated script.
// This repository enforces extensionless internal routes and extensionless canonical/og:url values.
// Keeping this file only as a guardrail so it can't be accidentally run to regress SEO.

console.error('scripts/fix-meta-tags.js is deprecated and intentionally disabled.');
console.error('Canonical and OG URLs must remain extensionless.');
process.exit(1);
